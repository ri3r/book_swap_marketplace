import React, { useState } from 'react';
import { 
  MapPin, Home, Search, Plus, User, 
  Moon, Sun, BookOpen, Flame, Compass, Heart, Star,
  Bell, Play
} from 'lucide-react';

// Mock data updated to English
const MOCK_BOOKS = [
  {
    id: 1,
    title: "Berserk Deluxe Vol. 1",
    author: "Kentaro Miura",
    condition: "Good",
    type: "swap",
    value: "For Swap",
    location: "Wurzburg",
    imageUrl: "https://images.unsplash.com/photo-1614726365723-419b4421f181?auto=format&fit=crop&w=200&q=80", 
  },
  {
    id: 2,
    title: "The Master and Margarita",
    author: "Mikhail Bulgakov",
    condition: "Good",
    type: "price",
    value: "€5.00",
    location: "Berlin",
    imageUrl: "https://images.unsplash.com/photo-1544947950-fa07a98d237f?auto=format&fit=crop&w=200&q=80",
  },
  {
    id: 3,
    title: "The Lord of the Rings",
    author: "J.R.R. Tolkien",
    condition: "Very Good",
    type: "price",
    value: "€8.50",
    location: "Munich",
    imageUrl: "https://images.unsplash.com/photo-1608146193798-13658eb5b625?auto=format&fit=crop&w=200&q=80",
  },
  {
    id: 4,
    title: "Clean Code",
    author: "Robert C. Martin",
    condition: "Like New",
    type: "swap",
    value: "For Swap",
    location: "Berlin",
    imageUrl: "https://images.unsplash.com/photo-1555066931-4365d14bab8c?auto=format&fit=crop&w=200&q=80",
  }
];

const CATEGORIES = [
  { id: 1, name: "Trending", icon: <Flame size={20} />, color: "bg-orange-100 text-orange-600 dark:bg-orange-900/30 dark:text-orange-400" },
  { id: 2, name: "Fiction", icon: <BookOpen size={20} />, color: "bg-blue-100 text-blue-600 dark:bg-blue-900/30 dark:text-blue-400" },
  { id: 3, name: "Romance", icon: <Heart size={20} />, color: "bg-pink-100 text-pink-600 dark:bg-pink-900/30 dark:text-pink-400" },
  { id: 4, name: "Sci-Fi", icon: <Star size={20} />, color: "bg-purple-100 text-purple-600 dark:bg-purple-900/30 dark:text-purple-400" },
  { id: 5, name: "Explore", icon: <Compass size={20} />, color: "bg-emerald-100 text-emerald-600 dark:bg-emerald-900/30 dark:text-emerald-400" },
];

export default function App() {
  const [isDarkMode, setIsDarkMode] = useState(false);
  const [activeTab, setActiveTab] = useState('home');

  const toggleTheme = () => setIsDarkMode(!isDarkMode);

  return (
    <div className={`${isDarkMode ? 'dark' : ''} min-h-screen w-full flex justify-center bg-gray-200 dark:bg-gray-950 font-sans`}>
      
      {/* Mobile App Container */}
      <div className="w-full max-w-md bg-[#F8F9FA] dark:bg-slate-900 flex flex-col h-screen overflow-hidden relative transition-colors duration-300">
        
        {/* Header - Greeting & Actions */}
        <header className="flex items-center justify-between px-6 pt-10 pb-4 z-10">
          <div className="flex items-center gap-3">
            <div className="w-12 h-12 rounded-full overflow-hidden border-2 border-white dark:border-slate-800 shadow-sm">
              <img 
                src="https://images.unsplash.com/photo-1438761681033-6461ffad8d80?auto=format&fit=crop&w=100&q=80" 
                alt="User Profile" 
                className="w-full h-full object-cover"
              />
            </div>
            <div>
              <p className="text-sm text-gray-500 dark:text-slate-400 font-medium">Good Morning,</p>
              <h1 className="text-xl font-bold text-gray-900 dark:text-white">Sophia</h1>
            </div>
          </div>

          <div className="flex items-center gap-2">
            <button 
              onClick={toggleTheme}
              className="w-10 h-10 flex items-center justify-center bg-white dark:bg-slate-800 rounded-full shadow-sm text-gray-700 dark:text-slate-300 hover:scale-105 transition-transform"
            >
              {isDarkMode ? <Sun size={20} /> : <Moon size={20} />}
            </button>
            <button className="w-10 h-10 flex items-center justify-center bg-white dark:bg-slate-800 rounded-full shadow-sm text-gray-700 dark:text-slate-300 hover:scale-105 transition-transform relative">
              <Bell size={20} />
              <span className="absolute top-2 right-2.5 w-2 h-2 bg-red-500 rounded-full border border-white dark:border-slate-800"></span>
            </button>
          </div>
        </header>

        {/* Scrollable Content */}
        <main className="flex-1 overflow-y-auto pb-28 px-6 space-y-8 hide-scrollbar">
          
          {/* Hero Card (Inspired by "Challenge with Pro Coach") */}
          <section className="mt-2">
            <div className="bg-[#E1F0F8] dark:bg-sky-900/20 rounded-[32px] p-6 relative overflow-hidden shadow-sm">
              <div className="relative z-10 w-2/3">
                <div className="inline-flex items-center gap-1.5 px-3 py-1 bg-white/60 dark:bg-white/10 rounded-full backdrop-blur-sm mb-3">
                  <Star size={14} className="text-sky-600 dark:text-sky-400" />
                  <span className="text-xs font-semibold text-sky-800 dark:text-sky-300">Staff Pick</span>
                </div>
                <h2 className="text-2xl font-bold text-gray-900 dark:text-white mb-1 leading-tight">
                  Harry Potter <br/> Collection
                </h2>
                <p className="text-sm text-gray-600 dark:text-slate-300 mb-4">
                  Available for swap in your area.
                </p>
                <button className="flex items-center gap-2 bg-white dark:bg-sky-500 text-gray-900 dark:text-white px-4 py-2 rounded-full font-semibold shadow-sm hover:scale-105 transition-transform text-sm w-fit">
                  View Details
                  <div className="w-6 h-6 rounded-full bg-gray-100 dark:bg-sky-400 flex items-center justify-center">
                    <Play size={12} className="ml-0.5" />
                  </div>
                </button>
              </div>
              
              {/* Decorative Book Image in Hero */}
              <div className="absolute -right-4 -bottom-6 w-40 h-48 rounded-xl transform rotate-[-10deg] shadow-lg">
                <img 
                  src="https://images.unsplash.com/photo-1551269901-5c5e14c25df7?auto=format&fit=crop&w=200&q=80" 
                  alt="Featured Book" 
                  className="w-full h-full object-cover rounded-xl"
                />
              </div>
            </div>
          </section>

          {/* Horizontal Categories (Inspired by "Select exercise type") */}
          <section>
            <h3 className="text-lg font-bold text-gray-900 dark:text-white mb-4">
              Select genre
            </h3>
            <div className="flex gap-4 overflow-x-auto pb-2 hide-scrollbar -mx-6 px-6">
              {CATEGORIES.map((cat) => (
                <div key={cat.id} className="flex flex-col items-center gap-2 shrink-0">
                  <button className={`w-16 h-16 rounded-full flex items-center justify-center ${cat.color} shadow-sm hover:scale-105 transition-transform`}>
                    {cat.icon}
                  </button>
                  <span className="text-xs font-medium text-gray-600 dark:text-slate-400">
                    {cat.name}
                  </span>
                </div>
              ))}
            </div>
          </section>

          {/* Book List (Soft UI Cards) */}
          <section>
            <div className="flex items-center justify-between mb-4">
              <h3 className="text-lg font-bold text-gray-900 dark:text-white">
                Near You
              </h3>
              <button className="text-sm font-medium text-sky-600 dark:text-sky-400 hover:underline">
                See All
              </button>
            </div>
            
            <div className="space-y-4">
              {MOCK_BOOKS.map((book) => (
                <div 
                  key={book.id} 
                  className="bg-white dark:bg-slate-800 rounded-3xl p-3 flex gap-4 shadow-[0_2px_10px_rgba(0,0,0,0.03)] dark:shadow-none hover:shadow-[0_8px_20px_rgba(0,0,0,0.06)] transition-all"
                >
                  <div className="shrink-0">
                    <img 
                      src={book.imageUrl} 
                      alt={book.title} 
                      className="w-20 h-28 object-cover rounded-2xl shadow-sm"
                    />
                  </div>

                  <div className="flex flex-col justify-center flex-1 py-1 pr-2">
                    <h4 className="font-bold text-gray-900 dark:text-white text-[15px] leading-tight mb-1 truncate">
                      {book.title}
                    </h4>
                    <p className="text-xs text-gray-500 dark:text-slate-400 mb-3">
                      {book.author}
                    </p>

                    <div className="flex items-center justify-between mt-auto">
                      <div className="flex flex-col gap-1.5">
                        <span className="inline-flex items-center px-2 py-0.5 rounded-lg text-[10px] font-bold bg-[#E8F5E9] text-green-700 dark:bg-green-900/30 dark:text-green-400 w-fit">
                          {book.condition}
                        </span>
                        <div className="flex items-center text-[11px] text-gray-500 dark:text-slate-400 font-medium">
                          <MapPin size={12} className="mr-1" />
                          {book.location}
                        </div>
                      </div>

                      <div className="text-right">
                         <span className={`text-sm font-bold ${book.type === 'swap' ? 'text-orange-500 dark:text-orange-400' : 'text-sky-600 dark:text-sky-400'}`}>
                          {book.value}
                        </span>
                      </div>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </section>

        </main>

        {/* Floating Bottom Navigation */}
        <div className="absolute bottom-6 left-0 right-0 px-6 z-20">
          <nav className="bg-white dark:bg-slate-800 rounded-3xl shadow-[0_8px_30px_rgba(0,0,0,0.08)] dark:shadow-none border border-white/20 dark:border-slate-700/50 flex justify-around items-center py-2 px-2">
            <NavButton 
              icon={<Home size={24} />} 
              isActive={activeTab === 'home'} 
              onClick={() => setActiveTab('home')} 
            />
            <NavButton 
              icon={<Search size={24} />} 
              isActive={activeTab === 'search'} 
              onClick={() => setActiveTab('search')} 
            />
            
            {/* Prominent Add Button */}
            <div className="relative -top-5">
              <button 
                onClick={() => setActiveTab('add')}
                className="bg-gray-900 dark:bg-sky-500 text-white w-14 h-14 rounded-full flex items-center justify-center shadow-lg hover:scale-105 transition-transform"
              >
                <Plus size={28} />
              </button>
            </div>

            <NavButton 
              icon={<BookOpen size={24} />} 
              isActive={activeTab === 'books'} 
              onClick={() => setActiveTab('books')} 
            />
            <NavButton 
              icon={<User size={24} />} 
              isActive={activeTab === 'profile'} 
              onClick={() => setActiveTab('profile')} 
            />
          </nav>
        </div>

      </div>

      {/* Global Style for hiding scrollbars but keeping functionality */}
      <style dangerouslySetInnerHTML={{__html: `
        .hide-scrollbar::-webkit-scrollbar {
          display: none;
        }
        .hide-scrollbar {
          -ms-overflow-style: none;
          scrollbar-width: none;
        }
      `}} />
    </div>
  );
}

function NavButton({ icon, isActive, onClick }) {
  return (
    <button 
      onClick={onClick}
      className={`p-3 rounded-2xl transition-all duration-200 ${
        isActive 
          ? 'bg-sky-50 dark:bg-sky-900/30 text-sky-600 dark:text-sky-400' 
          : 'text-gray-400 dark:text-slate-500 hover:text-gray-600 dark:hover:text-slate-300'
      }`}
    >
      {icon}
    </button>
  );
}